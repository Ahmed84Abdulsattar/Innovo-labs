<!-- This checklist appears automatically when you open a pull request. -->

## What & why
<!-- One or two sentences: what does this change do, and why? -->


## Type of change
- [ ] Feature (new functionality)
- [ ] Fix (bug fix)
- [ ] Chore (deps, config, tooling)
- [ ] Docs

## Checklist
- [ ] `npx tsc --noEmit` is clean (typecheck)
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] No secrets or `.env` values committed
- [ ] If the UI changed: checked on mobile (no clipping/overflow) and in dark mode

## Database / deployment notes
<!-- List any Supabase migrations that must be run (e.g. "run 052_*.sql before
     merging"), env vars to add in Vercel, or "none". Remember: merging to master
     deploys to production. -->
none

## Screenshots (UI changes only)
<!-- Before / after, desktop and mobile if relevant. -->
